server:
  port: 0

spring:
  application:
    name: stock-service

  profiles:
    group:
      test: test
      dev: dev
      prod: prod
    active: prod

  config:
    import:
      - msa-core.yml
      - monitor.yml

---
spring:
  config:
    activate:
      on-profile: dev

    import: file:.env[.properties]
